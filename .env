# .env.example — copy to .env for local development
# Set these values in Railway → Your Service → Variables for production.
# DO NOT commit .env — it is listed in .gitignore.

# ── MongoDB Atlas ────────────────────────────────────────────────────────────
# Get from: MongoDB Atlas → Your Cluster → Connect → Drivers → Connection String
# Replace <username>, <password>, and <dbname> with your actual values.
MONGODB_URI=mongodb+srv://newsbie:Stx2MWWUYJUatQZl@cluster0.56qb65z.mongodb.net/?appName=Cluster0

# ── JWT Secret ───────────────────────────────────────────────────────────────
# Generate a strong secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=fe06d23de8d6781dc91952faa35045eadd9354f567cc66f63d05fa851df19072

# ── Port ─────────────────────────────────────────────────────────────────────
# Railway sets PORT automatically — do NOT set it in the Railway dashboard.
# For local development only:
PORT=5000
